{
  "name": "aws-sls-tester",
  "version": "0.1.0",
  "description": "> Create an http endpoint that will create a message, place it on a queue that   will in turn be processed. The http endpoint will respond with the details of   the event that called it.",
  "main": "handler.js",
  "dependencies": {
    "aws-sdk": "^2.657.0"
  },
  "devDependencies": {
    "standard": "^14.3.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\""
  },
  "author": "st3v3nhunt",
  "license": "ISC"
}
